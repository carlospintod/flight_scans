/** Client-safe constants shared by the creation form (client component)
 *  and the server-side capacity math — no imports, no server deps. */
export const RUNS_PER_MONTH = 13; // Mon/Wed/Sat cadence
